#include <iostream>
#include <string>
#include <vector>

using namespace std;

void zipper(string a, string b, string c) {

}

int main()
{
	int N;
	cin >> N;
	string line
		vector< vector<string> >data;
	for (int i = 0; i < N; i++) {
		vector<string> row;
		string word;
		while (cin >> word) {
			row.push_back(word);
		}
		data.push_back(row);
	}
	for(auto iter=data.be)
}