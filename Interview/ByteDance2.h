#pragma once
#include <vector>
#include <string>
#include <set>
using namespace std;

class ByteDance2
{
public:
	ByteDance2();
	~ByteDance2();
	int longest_unique_substring(string data);
	int find_bu(vector<vector<int>> data);
	int restore_ip(string data);
	bool vliad_utf8(string data);
	int famous_user(vector<vector<int>> data);
};

